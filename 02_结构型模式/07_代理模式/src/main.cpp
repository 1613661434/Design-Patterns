#include "Communication.hpp"
using namespace std;

int main() {
    // =========================================================================
    // 场景1: 直接交流 (不使用代理)
    // =========================================================================
    Communication* comm = new Speaker;
    comm->communicate();
    delete comm;

    cout << "===================================" << endl;

    // =========================================================================
    // 场景2: 通过电话虫间接交流 (使用代理)
    // =========================================================================
    comm = new DenDenMushi;
    comm->communicate();
    delete comm;

    return 0;
}
